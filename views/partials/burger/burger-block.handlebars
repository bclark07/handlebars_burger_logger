<li>
	{{burger_name}}

	<button class="change-state" data-id="{{id}}" data-new="{{devoured}}">
		{{#if devoured}}All gone{{else}}Available!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete</button>
</li>