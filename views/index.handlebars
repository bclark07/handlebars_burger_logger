<h1>Enter the name of a burger you would like to try, then click the "Add" button.</h1>

<h2>Would Like to try:</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burger/burger-block devoured=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>I've had it before:</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{> burger/burger-block devoured=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a burger:</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burg">What is the name of the burger?</label>
    <input type="text" id="burg" burger_name="burger_name">
  </div>

  <div class="form-group">
    <label for="rest">What restaurant is it from?</label>
    <input type="text" id="rest" burger_name="restaurant_name">
  </div>

  <div class="form-group">
    <label for="devoured">Did you eat it already?</label><br>
    <input type="radio" name="devoured" value="1" checked> Yes<br>
    <input type="radio" name="devoured" value="0"> No
  </div>

  <button type="submit">Add the Burger</button>
</form>